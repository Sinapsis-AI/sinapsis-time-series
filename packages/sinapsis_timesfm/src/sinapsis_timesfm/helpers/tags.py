# -*- coding: utf-8 -*-
from enum import Enum


class Tags(Enum):
    TIMESFM = "TimesFM"
    DATAFRAMES = "dataframes"
    FORECASTING = "forecasting"
    PANDAS = "pandas"
    TIME_SERIES = "time_series"
